# Project Working Rules

## 작업 시작과 사실 확인

- 새 작업을 시작할 때 루트의 `CODEX_CONTEXT.md`를 먼저 읽고 이전 상태, 결정, 미해결 문제를 파악한다. 필요한 경우 `README.md`, `CONTENT-AUDIT.md`와 대상 코드를 이어서 읽는다.
- 실제 프로젝트 파일의 현재 상태를 우선적인 사실로 취급한다. 컨텍스트 문서와 코드가 충돌하면 코드를 확인해 판단하고 문서를 현재 상태로 수정한다. 사용자 요구와 구현 상태는 구분한다.
- `git status --short`를 확인하고 기존 변경을 보존한다. 다른 PC에서는 이전 서버, 도구, 절대 경로, 브라우저 세션이 유지된다고 가정하지 않는다.
- 문서의 TODO는 작업 이력이지 모든 작업을 즉시 실행하라는 권한이 아니다. 현재 사용자 요청 범위 안에서 진행한다.

## 지속적인 컨텍스트 관리

- 중요한 기능 구현, 버그 수정, 구조·의존성·설정 변경 또는 기술적 결정이 생기면 같은 작업 안에서 `CODEX_CONTEXT.md`를 갱신한다.
- 완료 항목은 '현재 진행 중인 작업'과 '다음 작업'에서 제거하고 완료 상태 또는 최근 기록에 요약한다. 검증 방법과 결과, 검증하지 못한 범위를 함께 남긴다.
- 새 문제와 TODO는 적절한 항목에 추가한다. 불확실한 사실은 '확인 필요'로 표시하고 근거와 확인 방법을 남긴다.
- 사소한 문구 수정이나 모든 도구 실행을 나열하지 않는다. 오래된 설명은 현재 상태로 교체하고 중복 기록은 통합한다. 최근 기록은 중요한 변경만 짧게 유지한다.
- API 키, 토큰, 비밀번호, 개인정보 등 민감한 정보는 절대로 `CODEX_CONTEXT.md`에 기록하지 않는다. 공개용 연락처도 여기 복제하지 말고 해당 코드 파일을 참조한다. 대화 전문, 인증 정보, 개인 PC 경로를 복사하지 않는다.
- 문서는 한국어 중심의 UTF-8 Markdown으로 작성하고, 프로젝트 내부 참조는 상대 경로를 사용한다. 특정 PC의 Codex 플러그인이나 전역 설정에 의존하는 절차를 필수 조건으로 만들지 않는다.
- 컨텍스트 파일을 Git에서 제외하지 않는다. Git 추가·커밋·원격 반영 상태와 사이트 배포 상태를 구분해 보고한다. 커밋·push·배포는 요청 없이 수행하지 않는다.

## 이 프로젝트의 구현·검증 규칙

- 빌드 없는 HTML/CSS/JavaScript 구조를 유지한다. 별도 프레임워크 도입은 사용자와 먼저 협의한다.
- 공통 메뉴·메타데이터 변경 시 6개 HTML을 함께 확인한다. CSS는 `styles.css` → `content.css` → `pages.css` 순서이며 기존 전역 선택자의 영향을 확인한다.
- 실제 학생 정보는 제공·공개 승인 전까지 참고 자료에서 가져오지 않는다. 가상 프로필과 실제 졸업생 성과를 연결하지 않는다.
- 교수·연구 내용 수정 시 `CONTENT-AUDIT.md`를 확인한다. 날짜·진행 상태·발주기관·국방 R&D 분류를 추정하지 않는다.
- UI 변경 후 데스크톱·모바일, 키보드 탐색, 링크와 앵커, 고정 메뉴 가림 및 가로 넘침을 확인한다. 캐시로 이전 화면이 보일 수 있으므로 응답과 최신 파일을 대조한다.
- 배포 워크플로는 루트 전체를 업로드하므로 프로젝트 문서도 공개될 수 있다. 비공개 참고 자료를 무단 복사하거나 저장소에 추가하지 않는다.

## 규칙의 이식성

이 파일과 `CODEX_CONTEXT.md`는 프로젝트 폴더와 함께 이동한다. 새 PC에서 프로젝트 루트를 열어 두 문서를 읽고 실제 파일을 대조하면 작업을 이어갈 수 있다. 규칙이 읽히지 않는 환경에서는 두 파일을 직접 읽도록 요청한다.

설정 근거: [OpenAI 공식 AGENTS.md 안내](https://learn.chatgpt.com/docs/agent-configuration/agents-md). 이는 파일 기반 작업 규칙이며 백그라운드 자동 동기화나 대화 복제가 아니다.
