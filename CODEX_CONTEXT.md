# Project Context

최종 갱신: 2026-09-11 (한국 시간)
기준: 실제 프로젝트 파일과 로컬 Git 상태. 이 문서는 인수인계용이며 코드가 우선한다.

## 먼저 확인할 상태

**구성원 등록·UI 개선은 구현 커밋 `2c64efa`로 main에 push했고 GitHub Pages 배포와 공개 파일 반영을 확인했다. 다른 PC에서는 최신 main을 clone/pull하여 이어갈 수 있다.**

- 구현 커밋: `2c64efa`(실제 구성원 프로필 등록 및 구성원 탐색 UI 개선). 실제 상세 HTML 4개와 사진 4개를 포함하며 기존 가상 상세 4개 삭제도 반영했다.
- 배포 검증: [GitHub Actions 실행 34520071788](https://github.com/ClCl616/aidis-lab-homepage/actions/runs/34520071788), 결과 success. 같은 커밋의 pages build and deployment도 성공했다.
- 공개 사이트: https://clcl616.github.io/aidis-lab-homepage/ — HTML 12개·사진 4개·pages.css·members.js 총 18개 응답을 로컬 파일과 대조해 일치 확인(텍스트 줄바꿈 차이 제외).
- 이 문서는 배포 결과를 기록하는 후속 문서 커밋이다. 정확한 최신 HEAD와 작업 트리 상태는 `git log -3 --oneline`, `git status --short --branch`로 확인한다.
- 사용자 요청으로 이번 커밋·push·배포를 수행했다. 이후 새 변경의 커밋·push·배포는 해당 요청 범위에서 판단한다. `main` push는 GitHub Pages 배포를 유발한다.

## 다른 PC의 새 채팅에서 시작

1. 원격 저장소의 최신 `main`을 clone한다. 기존 저장소라면 로컬 변경을 먼저 확인·보존한 뒤 최신 main을 받는다. 실제 구성원 HTML·사진은 모두 Git 추적 대상이므로 별도 임시 첨부 파일 없이 이어갈 수 있다.
2. 프로젝트 루트를 열고 `CODEX_CONTEXT.md`와 `AGENTS.md`를 읽는다. `git status --short --branch`, `git log -3 --oneline`, `git diff --stat`, `git ls-files --others --exclude-standard`로 실제 상태를 대조한다. 기존 변경을 덮어쓰거나 무조건 pull하지 않는다.
3. Python 3가 있다면 루트에서 `python -m http.server 4173 --bind 127.0.0.1`로 미리보기한다. 환경에 따라 `py -3` 또는 `python3`를 사용한다. 포트가 사용 중이면 서버 루트부터 확인한다.
4. `http://127.0.0.1:4173/members.html`에서 시작한다. 기존 PC의 서버·브라우저 탭·임시 스크립트·런타임 경로가 유지된다고 가정하지 않는다.
5. 다음 수정은 사용자의 추가 자료나 피드백 범위에서 진행한다. 아래 보완 목록은 자동 실행 요청이 아니다.

새 채팅 시작 문구 예: “AGENTS.md와 CODEX_CONTEXT.md를 읽고 실제 Git·파일 상태를 대조해줘. 최신 배포 상태와 로컬 변경을 확인하고 다음 요청을 이어갈 준비를 해줘.”

## 구조와 파일

대전대학교 AIDIS Lab의 빌드 없는 HTML/CSS/순수 JavaScript 홈페이지다. 프레임워크·npm 의존성·백엔드·DB·관리자 화면은 없다. 실행을 위한 별도 설치나 빌드는 필요 없으며 Python은 미리보기 용도다.

| 파일 | 역할 |
| --- | --- |
| `index.html` | 홈·페이지 안내·수상 소식·예정 일정 |
| `about.html` | 연구실 소개·활동 방향·위치/문의 |
| `professor.html` | 교수 학력·경력·연구·대표 논문 |
| `research.html` | 통합된 세 연구 분야 |
| `members.html` | 실제 재학생 4명, 졸업생 빈 상태, 검색·탭 |
| `member-ahn-chiwoo.html` | 랩장 상세, 경력/활동 9건 |
| `member-lee-taegyu.html` | 개인 상세, 경력/활동 4건 |
| `member-yu-nagyeong.html` | 개인 상세, 경력/활동 2건 |
| `member-jeong-seonju.html` | 개인 상세, 경력/활동 4건·자격증 |
| `activities.html`, `activity-kpaas.html` | 활동 20건의 목록/필터, 기존 K-PaaS 상세 |
| `contact.html` | 연구실 위치·지도교수/랩장 공개 연락 경로 |
| `members.js` | 구성원 자동 정렬·검색·탭·URL 상태 복원 |
| `activities.js`, `lab-updates.js`, `navigation.js` | 활동 필터, 공개 예정 일정, 기존 홈 해시 호환 |
| `styles.css` → `content.css` → `pages.css` | 공통/콘텐츠/페이지 스타일, 이 순서로 로드 |
| `assets/members/` | 실제 사진 4개와 `.gitkeep` |
| `CONTENT-AUDIT.md`, `README.md` | 출처 대조 기록, 실행/등록 안내 |
| `.github/workflows/pages.yml` | main push/수동 실행 시 루트 전체 Pages 업로드 |

- HTML은 현재 12개다. 헤더/푸터는 각 HTML에 복제되어 있다. 메뉴는 홈 → 소개 → 지도교수 → 연구 → 구성원 → 활동 → CONTACT.
- 모든 HTML의 CSS 버전은 `pages.css?v=20260911-9`. 구성원 스크립트는 `members.js?v=20260911-3`.
- `pages.css`에는 UI 반복 개선에 따른 후행 덮어쓰기 규칙이 있다. 앞쪽 규칙만 보고 판단하지 말고 끝부분·미디어 쿼리·브라우저 계산 스타일을 확인한다. CSS 구조 정리는 별도 요청 없이 수행할 필수 작업이 아니다.
- 로고/파비콘은 `assets/aidis-mark.svg`, 공유 이미지는 얼굴 없는 `assets/og-aidis-lab.png`. 교수 사진과 실제 학생 사진은 본문용이며 공유 이미지는 유지했다.

## 구성원: 확정된 현재 동작과 표시

### 자료 원칙

- 사용자는 **홈페이지 등록용으로 보내는 정보·사진은 항상 공개 동의 YES로 처리**하도록 명시했다. 재확인하지 않는다. 미제공 사실은 추정하지 않는다.
- 개인정보·이메일·공개 연락처는 이 문서에 복제하지 않는다. 실제 값은 해당 개인 HTML과 `contact.html`을 참조한다.
- 실제 사진 4개는 프로젝트에 저장돼 있다. PPT 사진 대기는 해소됐다. 사진 원본은 변경하지 않고 CSS `object-fit`으로 표시했다.
- 실제 상세에서 가상 안내·예시 이메일·가상 경력·noindex를 제거했다. 포트폴리오는 모두 미제공으로 생략한다. 제공된 GitHub 링크만 사용한다.

### 목록과 UI

- 전원 컴퓨터공학과·1기. 사용자 확인 학년을 사용한다. 입학 연도에서 현재 학년을 계산하거나 해가 바뀌면 자동 진급시키지 않는다.
- 카드 순서: `member-ahn-chiwoo.html` → `member-lee-taegyu.html` → `member-yu-nagyeong.html` → `member-jeong-seonju.html`.
- `data-leader`(1 우선), `data-cohort`(오름차순), `data-entry-year`(오름차순)를 기준으로 각 그룹 안에서 자동 정렬한다. 누락 숫자는 후순위, 동률은 기존 순서 유지. HTML도 같은 순서로 저장했다.
- 화면에는 학과·학년·기수·랩장 배지를 표시한다. 입학 연도는 정렬 데이터로만 보존한다. 목록 한 줄 소개는 제거했으며 상세 소개는 유지한다.
- 사진·이름·학적·배지 전체가 하나의 프로필 링크다. 이름 옆 작은 화살표와 hover/키보드 포커스로 표시하며 별도 프로필 보기 버튼은 없다.
- 데스크톱은 인물/기술/관심 분야를 나란히 배치한 한 열 카드. 기술과 관심 분야는 서로 다른 색의 줄바꿈 태그이며 내용을 생략하지 않는다.
- 1101px 이상은 같은 높이 카드와 확대된 사진(너비 108px), 이름/학적 세로 중앙 배치를 사용한다. 1100px 이하는 내용별 높이, 800px 이하는 인물 아래 기술/관심, 520px 이하는 모두 세로 배치다. 과도한 공백을 피하려고 모바일 높이를 강제로 통일하지 않는다.
- 재학생/졸업생 탭은 기본 재학생이며 선택 그룹만 보인다. 인원수는 실제 카드에서 계산한다. 탭 자체는 고정하지 않고 공통 헤더만 고정한다.
- 검색 예시 문구 및 재학생 제목 오른쪽 안내는 사용자 요청으로 제거했다.

### 검색·탐색

- 검색은 카드 텍스트(이름·학적·기술·관심 등)의 부분 문자열 검색이다. NFKC 정규화·영문 대소문자 무시·공백 구분 단어 AND 조건을 사용한다.
- 선택 탭 안의 결과 수·검색 빈 상태·초기화/지우기 버튼·Escape를 지원한다. 지우면 입력창으로 포커스를 돌린다. 탭 인원수는 전체 등록 수다.
- 검색어는 URL `q`에 저장하며 상세 이동 후 뒤로 가기 및 재진입 시 복원한다. JavaScript 비활성 시 검색 UI는 숨기고 기본 재학생 목록과 링크는 제공한다.
- 탭은 방향키·Home/End, `#members`/`#alumni`, 뒤로 가기를 지원한다. 기존 `#achievements`는 졸업생 탭 선택으로 호환한다.

### 기술·경력 편집 기준

- 기술 스택은 언어·프레임워크·플랫폼·AI 도구·버전 관리 기술. Visual Studio/VS Code는 목록·상세에서 제외했다. 기술 항목의 Git/GitHub는 Git으로 통일하되 실제 GitHub 링크와 활동 설명은 유지한다. 랩장 관심 분야의 클라우드 컴퓨팅은 제거했다.
- 개인 경력/활동은 진행 중 우선, 같은 상태에서 마지막 확인 연월 최신순이다. 날짜 미제공은 후순위, 같은 날짜는 기존 순서 유지. **현재 HTML에 정렬해 저장한 것이며 개인 활동 자동 정렬 스크립트는 없다.** 추가 시 직접 같은 규칙으로 배치한다.
- `activity-N` ID는 순번을 재부여하지 않는다. 활동 목록이 이 ID로 연결하므로 순서 변경 시에도 보존한다.
- 랩장 상세의 일영/GDGoC 참여는 `2025.09 - 현재`. 회장/Organizer 1년 임기는 `2025.09 - 2026.08`, 임기 종료 후 현재 운영진으로 참여한다. 전환 월은 별도 추정하지 않았다. 소개·역할 이력·활동 목록 요약에 함께 반영했다.
- `member-ahn-chiwoo.html#activity-5` 제목은 ‘AIDIS Lab 연구실’이다. `#activity-7` ZeroWaste는 사용자 확인으로 **2025.07**이며 활동 목록에도 반영했다. 다른 인물의 유사 프로젝트 날짜를 임의로 변경하거나 같은 팀이라고 연결하지 않았다.

## 활동·홈·기존 연구 내용

- 활동 목록은 K-PaaS 1건 + 개인 활동 19건 = 20건. `development` 5건, `competition` 9건, `education` 6건, `presentation` 0건. 필터·건수·빈 상태 지원. 개인 항목은 해당 프로필의 활동 앵커로 이동한다.
- 활동 목록 자체는 입력된 HTML 순서이며 자동 날짜 정렬은 없다. 프로필 내용을 수정하면 활동 요약도 함께 확인한다.
- K-PaaS 수상은 졸업한 학부연구생들의 재학 중 성과·상금 200만원. 새 인물과 참여 관계가 확정되지 않아 연결하지 않았다. 정확한 수상일·결과물 링크·개인 기여는 추정하지 않는다.
- 홈에는 기존 K-PaaS 소식 1건과 예정 일정 영역. `labEvents = []`이며 실제 일정은 없다. 일정 입력 형식은 `{title, date, location, url}`, 날짜 `YYYY-MM-DD`. 한국 시간 오늘 포함 미래만 날짜순 최대 3건 표시한다. 관리자·학생 작성 화면이나 캘린더 연동은 없다.
- 소개는 홈과 중복되던 대형 로고/연구 카드를 제거한 간결한 두 열 편집(모바일 한 열)이다.
- 연구 분야는 데이터 분석 및 인사이트 도출 / 인공지능 모델 연구 / AI 기반 소셜 플랫폼 개발. 교수 주요 연구 분야는 MANET·Routing Protocol / 소셜 빅데이터 분석·SNA / AI로 별도 유지한다.
- 교수는 학력 4건, 현재 경력 7건, 펼칠 수 있는 이전 경력 11건, 연구 7건(진행 2·완료 5), 대표 논문 6편. 바로가기와 기간 형식은 기존 합의를 유지한다.
- 국방 R&D 전체 과제 ‘인공지능 체계 신뢰성 시험 표준화 연구’와 대전대 담당 ‘인공지능 영상 신뢰성 평가 표준화 연구’를 중복 집계하지 않는다. 국방대 주관·고려대 자율주행·대전대 영상 AI 역할은 사용자 확인이다. `2025.09 - 현재`는 사용자 지정 표기이며 계약상 정확한 기간/기관은 확인 대상이다.
- 자료 근거는 `CONTENT-AUDIT.md`를 읽는다. 교수 경력/오픈랩의 현재 여부는 기존 자료 기준으로 실시간 검증된 사실이 아니다. 대표 6편과 과거 전체 41편 대조 기록을 구분한다.
- 기존 홈 해시 about/professor/research/members/contact는 개별 페이지로, projects는 `professor.html#projects`로 연결한다.

## 남은 작업·자료 의존성

- 추가 구성원·활동 자료는 사용자 제공 후 반영한다. 등록된 4명의 사진이나 공개 동의를 다시 요청할 필요는 없다.
- 랩장 교직과정/연구실 활동 시작 시점, 구체적 연구실 성과, Cloud Study Jam 공식 과정별 수료·배지는 미확인이다. 개인 학습 기록상 수강 완료를 공식 수료로 확대하지 않는다.
- `member-lee-taegyu.html`의 수상 2건은 연도/역할/연결 프로젝트 미제공으로 해당 항목을 생략했다.
- 교수 연구의 정확한 계약 기간·기관, ‘현재’ 경력/오픈랩의 최신 상태는 필요 시 출처와 대조한다.
- 참고 PPT는 프로젝트 밖 `../hompage-ref/2026_aidis_홍보데이(최종).pptx`(자료일 2026.03.19)로 기록돼 있으나 현재 PC의 해당 경로와 Projects 아래에서는 찾지 못했다. 원본 검토가 필요한 새 요청에서만 재첨부/경로가 필요하다. 사진은 후속 첨부로 해결돼 PPT가 현 작업의 차단 요인이 아니다.
- PPT 2쪽 연구경력은 이미지, 9쪽 랩 소개, 11쪽 수상, 12쪽 영상 연구다. PPT는 Git에 포함되지 않으며 무단으로 저장소에 넣지 않는다.

## 검증과 재검증 방법

- 이번 정리에서는 파일/JS/CSS/워크플로와 로컬 Git 상태를 읽고 HTML 12개·사진 4개·개인 활동 19개·목록 분류 20개·메타 버전·ZeroWaste 날짜를 대조했다. 문서만 정리했으며 UI 코드는 바꾸지 않았다.
- 직전 구현 검증: 12페이지 × 1440/390/320px, 내부 참조 272개, 중복 ID, HTTP 응답/파일 일치, 가로 넘침, 탭·활동 필터·상세 왕복·고정 메뉴/앵커 가림 통과. 브라우저 JavaScript 오류 없음.
- 검색의 한글/대소문자/복수 단어·빈 결과·초기화/포커스·Escape·상세 왕복 복원·JS 비활성 목록, 정렬 우선순위/누락값을 별도로 확인했다. UI 변경 단계마다 중간 폭 1024/800px 등과 데스크톱/모바일 스크린샷도 확인했다.
- 배포 직전 전체 12페이지 × 1440/390/320px 검사를 재실행했다. 내부 참조 272개, 응답 일치·넘침·중복 ID·키보드 탭·상세 왕복·활동 필터·앵커 가림 검사를 통과했으며 JavaScript 오류가 없었다. `git diff --cached --check`도 통과했다.
- 검증은 당시 사용 가능한 Playwright·Edge로 실행했다. 테스트 의존성/자동 테스트 스위트를 프로젝트에 추가하지 않았다. 임시 스크립트·스크린샷은 저장소 밖이며 다른 PC에 전달되지 않는다. 특정 PC의 플러그인/절대 경로를 필수로 사용하지 않는다.
- 새 환경에서는 로컬 서버를 띄운 뒤 브라우저 개발자 도구 또는 사용 가능한 자동화 도구로 위 동작을 재검증한다. CSS 순서/버전, 내부 href/src의 파일 및 앵커 존재, noindex 유무, 모바일 넘침과 키보드 포커스를 확인한다. 캐시가 의심되면 고유 review 쿼리를 붙이고 응답과 파일을 비교한다.
- `git diff --check`를 확인하고, 사용자 요청으로 스테이징할 때는 `git diff --cached --check`도 확인한다.
- 미검증: 실기기·다른 브라우저 엔진·스크린리더·메신저 캐시.

## 배포·문서 취급

`.github/workflows/pages.yml`은 main push 또는 수동 실행으로 루트 전체를 업로드한다. 문서도 공개될 수 있으므로 비공개 원본·연락처 모음·인증정보·임시 테스트 파일을 추가하지 않는다. AGENTS.md와 이 문서는 Git 추적 대상이다. 소스 커밋, 원격 push, Pages 배포 성공은 각각 구분해 보고한다.

## 주요 작업 기록

- 2026-09-09~10: 다중 페이지·교수 출처 대조·연구 통합·소개 개편. 구현 `0aaac2f`와 후속 문서 `74134a5` 반영 이력.
- 2026-09-11: 실제 구성원/사진 등록·개인 활동과 랩장 연락 경로 반영, 가상 프로필 제거. 목록 탐색·검색·자동 정렬·반응형 카드 UI 개선.
- 2026-09-11: 개인 경력 최근순 정렬·현재 운영진 참여 반영·ZeroWaste 2025.07 확정. 인수인계 문서를 현재 파일에 맞춰 재정리한 뒤 구현 `2c64efa` push·Pages 배포·공개 파일 일치 검증 완료.
