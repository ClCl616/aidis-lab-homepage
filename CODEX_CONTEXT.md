# Project Context

최종 갱신: 2026-09-15 (한국 시간)
기준: 실제 프로젝트 파일·Git·공개 배포 상태. 코드가 우선하며 사용자 요구와 구현 상태를 구분한다.

## 현재 상태와 다음 작업

- PC 중심 UI, 홈 대표 카드, 카테고리 메뉴, 뉴스/일정 패널, 공통 푸터 개선은 **배포 완료**다. 미완료 구현 작업은 없으며 사용자 피드백을 기다린다.
- 구현 커밋 `bee640b`, 배포 기록 커밋 `35ad4ab`. 이번 문서 정리 시작 시 로컬 HEAD와 실제 원격 `main`은 `35ad4abe6dfec67c719de6853b4ec2dde41e0426`으로 일치했고 작업 트리는 깨끗했다.
- 최신 배포: [워크플로 34876658601](https://github.com/ClCl616/aidis-lab-homepage/actions/runs/34876658601), Pages 실행 34876657388 모두 `35ad4ab` 기준 success. 공개 HTML 15개·CSS 3개·JS 4개 총 22개 파일이 로컬과 일치했다(줄바꿈 제외).
- 공개 사이트: https://clcl616.github.io/aidis-lab-homepage/
- 저장소: https://github.com/ClCl616/aidis-lab-homepage (기본 작업 브랜치 `main`).
- 정리한 컨텍스트 문서는 사용자의 후속 push 요청에 따라 커밋·원격 반영한다. 다른 PC에서는 최신 main을 받아 이 문서와 AGENTS.md부터 읽는다. 사이트 코드는 변경하지 않았으며 문서 push도 Pages 워크플로를 실행한다. 정확한 최신 커밋·배포 결과는 Git과 Actions에서 확인한다.
- 현재 HTML 15개, 재학생 7명, 개인 상세 7개, 실제 학생 사진 5개, 활동 21건. 졸업생 목록과 예정 일정은 비어 있다.
- 모바일 디자인은 사용자가 후속 작업으로 미뤘다. 다음 변경은 새 요청 범위에서 진행하며 아래 자료 의존성을 자동 실행 TODO로 취급하지 않는다. 커밋·push·배포는 요청 없이 하지 않는다.

## 다른 PC의 새 Codex 채팅에서 시작

1. 이 최신 문서를 포함한 프로젝트 폴더를 열거나 원격 저장소를 clone한다. 기존 폴더에서는 먼저 변경을 확인하고 보존한다. 무조건 pull/reset하지 않는다.
2. 루트의 `AGENTS.md`와 이 문서를 읽는다. `git status --short --branch`, `git log -3 --oneline`, `git diff --stat`, `git ls-files --others --exclude-standard`로 실제 상태를 대조한다. 필요 시 `git ls-remote origin refs/heads/main`으로 실제 원격을 확인한다.
3. 빌드/패키지 설치는 없다. 사용 가능한 Python 3로 루트에서 `python -m http.server 4173 --bind 127.0.0.1`을 실행한다. 환경에 따라 `py -3` 또는 `python3`를 사용한다. 이미 사용 중인 포트는 서버 루트부터 확인한다.
4. `http://127.0.0.1:4173/index.html`에서 시작한다. 이전 PC의 실행 서버·브라우저 세션·도구·임시 파일·절대 경로가 유지된다고 가정하지 않는다. 공개 사이트와 로컬 미리보기는 구분한다.
5. 대상 코드와 필요 시 `README.md`, `CONTENT-AUDIT.md`를 읽고 다음 사용자 요청을 처리한다. 공통 요소는 전체 HTML에 복제되어 있으므로 함께 확인한다.

새 채팅 시작 문구: “AGENTS.md와 CODEX_CONTEXT.md를 읽고 실제 Git·파일·배포 상태를 대조한 뒤 다음 작업을 이어갈 준비를 해줘.”

## 이번 작업의 최종 UI 결정

- PC 1000px 이상을 우선한다. 최대 1200px 중앙 레이아웃, 헤더 88px, 하위 페이지는 왼쪽 200px + 간격 32px + 본문이다. 1000–1199px는 메뉴 170px·간격 24px다. 본문은 주로 14–15px, 하위 제목 28px, 홈 대표 제목 32px다.
- 하위 14페이지는 `.academic-layout`, `.section-menu`, `.academic-content`, 현재 위치 표시를 사용한다. 본문 건너뛰기는 `#page-content`, 홈은 `#main`이다.
- **세부 메뉴는 실제 카테고리만 사용한다.** 본문 소제목·개별 활동을 카테고리처럼 나열하지 않는다. 소개/문의는 연구실 소개·위치 및 문의, 구성원/개인 상세는 재학생·졸업생, 활동/수상 상세는 전체·개발·공모전/수상·교육/운영·발표/공유다. 교수·연구는 왼쪽 섹션 제목만 있고 하위 메뉴는 없다.
- PC의 중복 본문 구성원 탭·활동 필터는 숨긴다. 1000px 미만에서는 사이드 메뉴를 숨기고 기존 본문 컨트롤을 사용한다. 활동 분류는 URL 해시로 직접 진입·새로고침·뒤로 가기 복원을 지원한다.
- 홈은 긴 설명보다 대표 카드 중심이다. 연구(큰 네트워크 카드)·구성원·K-PaaS 수상·활동·지도교수 총 5개. 각 카드 전체가 링크이고 짧은 제목·설명과 CSS/SVG 그래픽을 사용한다. 장식 네트워크는 가장자리를 의도적으로 잘라 표시한다.
- 홈 하단은 **Notice & News / Calendar** 두 열 패널이다. 전체 활동 링크·UPCOMING·별도 문의 유도 링크는 사용자 요청으로 제거했다. 뉴스에 미확인 날짜를 넣지 않는다.
- 공통 푸터는 15페이지 모두 중앙 정렬 로고·영문 정식 명칭·기존 공개 위치·저작권 표기다. 실제 위치/연락처는 `contact.html`과 각 HTML을 참조하며 이 문서에 복제하지 않는다.
- 구성원은 사진/학적 왼쪽, 기술/관심 오른쪽의 직사각형 목록이다. 사진 80×104px(1000–1199px는 68×88px), 카드 높이는 내용에 따른다. 검색창 placeholder는 삭제했고 레이블·기능은 유지했다.
- 활동 목록은 한 열, 문의는 2×2다. 교수·개인 상세는 사진과 정보 뒤에 구분선으로 이력을 정리한다. 기존 인물·연구 내용과 사진 원본은 보존했다. 참고 사이트의 인물·이미지·연락처는 가져오지 않았다.

## 구조와 파일

대전대학교 AIDIS Lab의 빌드 없는 HTML/CSS/순수 JavaScript 홈페이지다. 프레임워크·npm 의존성·백엔드·DB·관리자 화면은 없다. 실행을 위한 별도 설치나 빌드는 필요 없으며 Python은 미리보기 용도다.

| 파일 | 역할 |
| --- | --- |
| `index.html` | 홈·페이지 안내·수상 소식·예정 일정 |
| `about.html` | 연구실 소개·활동 방향·위치/문의 |
| `professor.html` | 교수 학력·경력·연구·대표 논문 |
| `research.html` | 통합된 세 연구 분야 |
| `members.html` | 실제 재학생 7명, 졸업생 빈 상태, 검색·탭 |
| `member-ahn-chiwoo.html` | 랩장 상세, 경력/활동 9건 |
| `member-lee-taegyu.html` | 개인 상세, 경력/활동 4건 |
| `member-yu-nagyeong.html` | 개인 상세, 경력/활동 2건 |
| `member-jeong-seonju.html` | 개인 상세, 경력/활동 4건·자격증 |
| `member-yoon-jongmin.html`, `member-choi-seoyeong.html` | 추가 구성원 상세, 경력·활동 미제공 |
| `member-choi-suyeon.html` | 최수연 상세, 대학 연구실 활동 1건 |
| `activities.html`, `activity-kpaas.html` | 활동 21건의 목록/필터, 기존 K-PaaS 상세 |
| `contact.html` | 연구실 위치·지도교수/랩장 공개 연락 경로 |
| `members.js` | 구성원 자동 정렬·검색·탭·URL 상태 복원 |
| `activities.js`, `lab-updates.js`, `navigation.js` | 활동 필터, 공개 예정 일정, 기존 홈 해시 호환 |
| `styles.css` → `content.css` → `pages.css` | 공통/콘텐츠/페이지 스타일, 이 순서로 로드 |
| `assets/members/` | 실제 사진 5개와 `.gitkeep` |
| `CONTENT-AUDIT.md`, `README.md` | 출처 대조 기록, 실행/등록 안내 |
| `.github/workflows/pages.yml` | main push/수동 실행 시 루트 전체 Pages 업로드 |

- HTML은 현재 15개다. 헤더/푸터는 각 HTML에 복제되어 있다. 메뉴는 홈 → 소개 → 지도교수 → 연구 → 구성원 → 활동 → CONTACT.
- 모든 HTML의 CSS 버전은 `pages.css?v=20260915-7`. 구성원 스크립트는 `members.js?v=20260915-1`.
- `pages.css`에는 UI 반복 개선에 따른 후행 덮어쓰기 규칙이 있다. 앞쪽 규칙만 보고 판단하지 말고 끝부분·미디어 쿼리·브라우저 계산 스타일을 확인한다. CSS 구조 정리는 별도 요청 없이 수행할 필수 작업이 아니다.
- 로고/파비콘은 `assets/aidis-mark.svg`, 공유 이미지는 얼굴 없는 `assets/og-aidis-lab.png`. 교수 사진과 실제 학생 사진은 본문용이며 공유 이미지는 유지했다.

## 구성원: 확정된 현재 동작과 표시

### 자료 원칙

- 사용자는 **홈페이지 등록용으로 보내는 정보·사진은 항상 공개 동의 YES로 처리**하도록 명시했다. 재확인하지 않는다. 미제공 사실은 추정하지 않는다.
- 개인정보·이메일·공개 연락처는 이 문서에 복제하지 않는다. 실제 값은 해당 개인 HTML과 `contact.html`을 참조한다.
- 실제 사진 5개는 프로젝트에 저장돼 있다. 최서영·최수연은 사진 미제공으로 자리표시자를 사용한다. PPT 사진 대기는 해소됐다. 사진 원본은 변경하지 않고 CSS `object-fit`으로 표시했다.
- 실제 상세에서 가상 안내·예시 이메일·가상 경력·noindex를 제거했다. 포트폴리오는 모두 미제공으로 생략한다. 제공된 GitHub 링크만 사용한다.

### 목록과 UI

- 전원 컴퓨터공학과·1기. 사용자 확인 학년을 사용한다. 입학 연도에서 현재 학년을 계산하거나 해가 바뀌면 자동 진급시키지 않는다.
- 카드 순서: `member-ahn-chiwoo.html` → `member-lee-taegyu.html` → `member-yu-nagyeong.html` → `member-yoon-jongmin.html` → `member-jeong-seonju.html` → `member-choi-seoyeong.html` → `member-choi-suyeon.html`.
- `data-leader`(1 우선), `data-cohort`(오름차순), `data-entry-year`(오름차순)를 기준으로 각 그룹 안에서 자동 정렬한다. 누락 숫자는 후순위, 동률은 기존 순서 유지. HTML도 같은 순서로 저장했다.
- 화면에는 학과·학년·기수·랩장 배지를 표시한다. 입학 연도는 정렬 데이터로만 보존한다. 목록 한 줄 소개는 제거했다. 제공된 상세 소개만 유지하며, 미제공 인물의 소개문은 추정하지 않는다.
- 사진·이름·학적·배지 전체가 하나의 프로필 링크다. 이름 옆 작은 화살표와 hover/키보드 포커스로 표시하며 별도 프로필 보기 버튼은 없다.
- 데스크톱은 인물/기술/관심 분야를 나란히 배치한 한 열 카드. 기술과 관심 분야는 서로 다른 색의 줄바꿈 태그이며 내용을 생략하지 않는다.
- 현재 PC에서는 내용별 높이 카드와 80×104px 사진(1000–1199px는 68×88px)을 사용한다. 인물 정보 옆에 기술·관심 분야를 두 행으로 정리한다. 이전 PC 카드 규칙은 마지막 학술 홈페이지 스타일 블록으로 대체한다. 1000px 미만은 기존 모바일 스타일을 유지한다.
- 기본 분류는 재학생이며 선택 그룹만 보인다. PC는 왼쪽 재학생/졸업생 링크, 1000px 미만은 본문 탭을 사용한다. 인원수는 실제 카드에서 계산해 본문 탭에 표시한다. 공통 헤더만 고정한다.
- 검색 예시 문구 및 재학생 제목 오른쪽 안내는 사용자 요청으로 제거했다.

### 검색·탐색

- 검색은 카드 텍스트(이름·학적·기술·관심 등)의 부분 문자열 검색이다. NFKC 정규화·영문 대소문자 무시·공백 구분 단어 AND 조건을 사용한다.
- 선택 탭 안의 결과 수·검색 빈 상태·초기화/지우기 버튼·Escape를 지원한다. 지우면 입력창으로 포커스를 돌린다. 탭 인원수는 전체 등록 수다.
- 검색어는 URL `q`에 저장하며 상세 이동 후 뒤로 가기 및 재진입 시 복원한다. JavaScript 비활성 시 검색 UI는 숨기고 기본 재학생 목록과 링크는 제공한다.
- 본문 탭은 방향키·Home/End를 지원하고, PC 카테고리 링크는 Tab/Enter로 접근한다. 두 UI는 `#members`/`#alumni`, 뒤로 가기와 선택 상태를 공유한다. 기존 `#achievements`는 졸업생 탭 선택으로 호환한다.

### 기술·경력 편집 기준

- 기술 스택은 언어·프레임워크·플랫폼·AI 도구·버전 관리 기술. Visual Studio/VS Code는 목록·상세에서 제외했다. 기술 항목의 Git/GitHub는 Git으로 통일하되 실제 GitHub 링크와 활동 설명은 유지한다. 랩장 관심 분야의 클라우드 컴퓨팅은 제거했다.
- 개인 경력/활동은 진행 중 우선, 같은 상태에서 마지막 확인 연월 최신순이다. 날짜 미제공은 후순위, 같은 날짜는 기존 순서 유지. **현재 HTML에 정렬해 저장한 것이며 개인 활동 자동 정렬 스크립트는 없다.** 추가 시 직접 같은 규칙으로 배치한다.
- `activity-N` ID는 순번을 재부여하지 않는다. 활동 목록이 이 ID로 연결하므로 순서 변경 시에도 보존한다.
- 랩장 상세의 일영/GDGoC 참여는 `2025.09 - 현재`. 회장/Organizer 1년 임기는 `2025.09 - 2026.08`, 임기 종료 후 현재 운영진으로 참여한다. 전환 월은 별도 추정하지 않았다. 소개·역할 이력·활동 목록 요약에 함께 반영했다.
- `member-ahn-chiwoo.html#activity-5` 제목은 ‘AIDIS Lab 연구실’이다. `#activity-7` ZeroWaste는 사용자 확인으로 **2025.07**이며 활동 목록에도 반영했다. 다른 인물의 유사 프로젝트 날짜를 임의로 변경하거나 같은 팀이라고 연결하지 않았다.

## 활동·홈·기존 연구 내용

- 활동 목록은 K-PaaS 1건 + 개인 활동 20건 = 21건. `development` 5건, `competition` 9건, `education` 7건, `presentation` 0건. 필터·건수·빈 상태 지원. 개인 항목은 해당 프로필의 활동 앵커로 이동한다.
- 활동 목록 자체는 입력된 HTML 순서이며 자동 날짜 정렬은 없다. 프로필 내용을 수정하면 활동 요약도 함께 확인한다.
- K-PaaS 수상은 졸업한 학부연구생들의 재학 중 성과·상금 200만원. 새 인물과 참여 관계가 확정되지 않아 연결하지 않았다. 정확한 수상일·결과물 링크·개인 기여는 추정하지 않는다.
- 홈 Notice & News는 정적 HTML의 K-PaaS 소식 1건이다(뉴스는 최대 3건을 유지하는 편집 기준이며 자동 제한 기능은 없다). Calendar는 일정 목록으로 월간 달력 UI가 아니다. `labEvents = []`이며 실제 일정은 없다. 일정 입력 형식은 `{title, date, location, url}`, 날짜 `YYYY-MM-DD`. 한국 시간 오늘 포함 미래만 날짜순 최대 3건 표시한다. 관리자·학생 작성 화면이나 캘린더 연동은 없다.
- 소개는 본문 소제목·문단 중심의 한 열 구성이고 마지막 함께하기 영역은 PC 두 열이다.
- 연구 분야는 데이터 분석 및 인사이트 도출 / 인공지능 모델 연구 / AI 기반 소셜 플랫폼 개발. 교수 주요 연구 분야는 MANET·Routing Protocol / 소셜 빅데이터 분석·SNA / AI로 별도 유지한다.
- 교수는 학력 4건, 현재 경력 7건, 펼칠 수 있는 이전 경력 11건, 연구 7건(진행 2·완료 5), 대표 논문 6편. 목차형 바로가기는 제거했지만 기존 섹션 ID와 기간 표기는 유지한다.
- 국방 R&D 전체 과제 ‘인공지능 체계 신뢰성 시험 표준화 연구’와 대전대 담당 ‘인공지능 영상 신뢰성 평가 표준화 연구’를 중복 집계하지 않는다. 국방대 주관·고려대 자율주행·대전대 영상 AI 역할은 사용자 확인이다. `2025.09 - 현재`는 사용자 지정 표기이며 계약상 정확한 기간/기관은 확인 대상이다.
- 자료 근거는 `CONTENT-AUDIT.md`를 읽는다. 교수 경력/오픈랩의 현재 여부는 기존 자료 기준으로 실시간 검증된 사실이 아니다. 대표 6편과 과거 전체 41편 대조 기록을 구분한다.
- 기존 홈 해시 about/professor/research/members/contact는 개별 페이지로, projects는 `professor.html#projects`로 연결한다.

## 남은 작업·자료 의존성

- 추가 구성원·활동 자료는 사용자 제공 후 반영한다. 등록된 7명의 공개 동의를 다시 요청하지 않는다. 기존 사진 5개는 저장소에 포함돼 있다. 최서영·최수연 사진은 추후 제공될 때 교체하며 현재 작업의 차단 요인이 아니다.
- 랩장 교직과정/연구실 활동 시작 시점, 구체적 연구실 성과, Cloud Study Jam 공식 과정별 수료·배지는 미확인이다. 개인 학습 기록상 수강 완료를 공식 수료로 확대하지 않는다.
- `member-lee-taegyu.html`의 수상 2건은 연도/역할/연결 프로젝트 미제공으로 해당 항목을 생략했다.
- 교수 연구의 정확한 계약 기간·기관, ‘현재’ 경력/오픈랩의 최신 상태는 필요 시 출처와 대조한다.
- 참고 PPT의 기존 상대 경로는 `../hompage-ref/2026_aidis_홍보데이(최종).pptx`(자료일 2026.03.19)다. 이전 작업에서 이 경로에 파일이 없음을 확인했다. 이번 문서 정리에서는 원본 존재를 재검증하지 않았다. 본문은 재검토하지 않았다. Git에 포함되지 않으므로 다른 PC에도 있다고 가정하지 않는다. 원본 검토가 필요한 새 요청에서만 경로 확인 또는 별도 전달이 필요하다.
- PPT 2쪽 연구경력은 이미지, 9쪽 랩 소개, 11쪽 수상, 12쪽 영상 연구다. PPT는 Git에 포함되지 않으며 무단으로 저장소에 넣지 않는다.

## 검증 범위와 재검증 방법

### 이번 문서 정리에서 직접 확인

- 로컬/실제 원격 main 일치, 시작 시 깨끗한 작업 트리, 최신 배포 2건 success.
- 공개 HTML/CSS/JS 22개와 로컬 일치. HTML 15개·구성원 7명·사진 5개·활동 분류별 수를 코드에서 확인했다.
- 전체 HTML CSS 버전 `pages.css?v=20260915-7`. 구성원/활동 스크립트는 각각 `members.js?v=20260915-1`, `activities.js?v=20260915-1`이다.
- 문서 수정만 수행했고 UI 검사를 새로 실행하지 않았다. `git diff --check`로 문서 변경을 확인한다.

### 구현 단계에서 확인한 사항

- PC 레이아웃 단계의 15페이지 × 1024/1366/1920px 45조합: 가로 넘침·이미지 로드 문제 없음. 이후 카테고리 수정은 8개 대표 페이지 1024px, 홈 카드 수정은 1024/1366/1920px에서 추가 검사했다.
- 홈 대표 카드/뉴스 Enter 상세 이동, 1440px 홈 시각 확인, 1024px 뉴스/일정 패널 같은 높이·가로 넘침 없음. 최종 푸터는 홈 시각 확인과 전체 15개 동일성 검사를 수행했다.
- 구성원 검색·검색어 유지·분류 선택 동기화, 활동 개발 5건·발표/공유 빈 상태·Enter·새로고침·뒤로 가기 확인. 교수 학력 앵커가 고정 헤더 아래에 보이는 것을 확인했다.
- 구현 중 내부 파일/앵커·중복 ID·diff 검사를 통과했다. 이후 활동 URL 해시는 DOM 앵커가 아닌 필터 상태이므로 정적 앵커 검사 시 구분해야 한다.
- 최수연 추가 당시 모바일 일부 화면 검증 이력은 있으나 **최종 PC 개편 전체의 모바일 검증을 대체하지 않는다.** 실기기·다른 엔진·스크린리더·메신저 캐시·일정이 실제 등록된 브라우저 화면은 미검증이다.

### 다음 변경 시

- 실제 정적 서버와 브라우저로 요청 대상 화면, 키보드, 링크/앵커, 고정 헤더 가림, 가로 넘침을 확인한다. 공통 메뉴·푸터·메타데이터 변경은 HTML 15개를 함께 대조한다.
- CSS 로드 순서와 후행 덮어쓰기·미디어 쿼리를 확인한다. 캐시가 의심되면 응답과 최신 파일을 비교한다.
- `git diff --check`, 스테이징 시 `git diff --cached --check`를 확인한다. 테스트 의존성/상설 자동 테스트 스위트는 없으며 특정 PC의 임시 검사 파일·플러그인을 필수 조건으로 만들지 않는다.

## 배포와 문서 취급

- `.github/workflows/pages.yml`: main push 또는 수동 실행으로 루트 전체를 GitHub Pages에 업로드한다. 문서만 push해도 배포가 실행된다.
- 소스 커밋·원격 push·Pages 성공·공개 파일 반영을 각각 확인하고 구분해서 보고한다. 후속 변경의 커밋/push는 사용자 요청 범위에서 수행한다.
- `AGENTS.md`와 이 문서는 Git 추적 대상이다. 문서도 공개되므로 비공개 원본·연락처 모음·인증정보·임시 파일을 추가하지 않는다.

## 주요 이력

- 2026-09-09~11: 다중 페이지·교수 출처 대조·연구 통합, 실제 구성원/사진·개인 활동 등록, 검색·정렬·탭 구현. 가상 프로필 제거.
- 2026-09-15: 최수연 목록·상세·연구실 활동 추가(`578f8c4`), 재학생 7명·HTML 15개·활동 21건으로 확장 후 배포.
- 2026-09-15: 사용자 피드백에 따라 PC 전체 UI·카테고리 메뉴·홈 카드·Notice & News/Calendar·공통 푸터 확정. 구현 `bee640b`, 기록 `35ad4ab` push 및 배포 성공.
- 2026-09-15: 실제 Git/공개 파일 기준으로 본 문서의 중복 기록·과거 수치·미배포 설명을 정리. 문서만 수정했으며 사용자의 후속 요청으로 커밋·push한다.
