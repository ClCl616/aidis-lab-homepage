# Project Context

최종 갱신: 2026-09-21 (한국 시간). 실제 파일이 우선이며 구현·Git 반영·배포 상태를 구분한다.

## 현재 상태와 다음 작업

- 2026-09-21 배포 후 UI 피드백 반영(사용자 재배포 요청으로 커밋·push·Pages 배포 진행): 활동 관리 링크를 관리자 전용으로 숨김 처리하고 소개 문구 오른쪽에 배치했다. 상단 CONTACT/로그인 두 링크를 Login 또는 표시 이름+님 버튼 하나로 통합했다. 마이페이지에서 표시 이름을 저장하며 미설정 시 연결 프로필 이름, 그 외 관리자/회원 표기를 사용한다. 이름 메타데이터는 표시용이며 권한 판단에는 사용하지 않는다. 홈 Notice & News의 K-PaaS 항목을 제거하고 빈 상태를 표시한다.
- 이태규 학적의 졸업 예정 문구 제거와 졸업생 전원 0기는 HTML·CMS catalog 및 실제 Supabase 저장값에 함께 반영했다. DB 변경은 공개 사이트에 적용했으며 레이아웃/헤더/홈 변경도 이번 재배포에 포함한다.
- 이번 검증: HTML 23개 상단 계정 링크 단일화, CMS 필드 대응, JS 구문·diff 검사, 관리자 링크 노출/비로그인 숨김, 졸업생 5명 0기·홈 빈 소식·이름 입력 화면을 확인했다. 모바일 390px 프레임 내부 너비/스크롤375px로 가로 넘침 없고 계정 버튼이 보인다.

- 2026-09-21: 이태규 프로필 사진을 새 사용자 제공 PNG 원본으로 교체하고 목록·상세에 연결했다. 통합 계정 기능과 함께 d052111 커밋으로 배포했다. 원본 해시·두 페이지 참조·기존 경로 제거와 diff 검사 및 공개 프로필 브라우저 사진을 확인했다.

- 현재 요청: 공개 열람을 유지하면서 회원가입·본인 프로필 편집·활동 제출·관리자 사이트 관리를 통합한다. 회원가입 후 관리자가 기존 프로필을 연결하며 활동은 승인 후 공개한다. 사용자는 별도 SMTP 없이 이메일 가입 인증을 끄고 관리자 전용 기능 설정 탭을 원한다.
- 통합 `account.html`과 공통 로그인 메뉴를 구현하고 Supabase DB migration 3개를 실제 적용했다. 프로필 연결 승인·해제, 본인/전체 프로필 편집, 사진 업로드, 활동 승인·반려·수정·복구 가능한 삭제, 사이트 본문 문구·링크·사진 편집, 운영 설정 4개를 지원한다. 기존 관리자 계정과 사진 교체를 보존했다.
- 가입은 활성화했고 인증 복귀 URL을 등록했다. 사용자가 Confirm email을 직접 끄고 저장했으며 실제 Auth API의 신규 가입 즉시 세션 발급·로그인·로그아웃으로 인증 없는 가입을 검증했다. SMTP는 연결하지 않았다.
- 운영 설정: 프로필 연결 신청 접수, 본인 프로필 수정, 구성원 활동 제출, 새 활동 공개 표시. 화면과 RLS에서 함께 검사한다. 설정은 회원가입 자체나 Auth 이메일 정책을 바꾸지 않는다.
- 일반/관리자/익명의 권한과 기능 비활성화는 실제 DB 트랜잭션 검사 후 롤백했다. 브라우저에서 기존 관리자 세션 이전, 콘텐츠·프로필·설정 저장, 활동 복구→승인 공개→삭제를 확인했다. 이전 검증용 활동은 삭제 상태로 되돌렸다. 2026-09-21 사용자 요청으로 d052111을 커밋·push했고 GitHub Pages 배포 성공을 확인했다.
- 기능 배포 커밋은 d052111이다. GitHub Pages 실행 35530349259가 성공했으며 공개 account.html·account.js·site-api.js·activity-api.js·CMS catalog·프로필 PNG가 로컬 파일과 일치했다. 후속 문서 커밋은 배포 검증 기록만 갱신한다.
- HTML 23개(통합 계정·관리자 이전 주소·새 활동 상세 포함), 재학생 7명·졸업생 5명, 개인 상세 12개, 실제 학생 사진 6개, 공개 활동 목록 K-PaaS 1건. 예정 일정은 없다.
- 당분간 **PC(1000px 이상) 중심으로 제작**한다. 기존 모바일 규칙은 남아 있지만 최종 모바일 개선은 보류다.
- 공개 사이트: https://clcl616.github.io/aidis-lab-homepage/
- 저장소: https://github.com/ClCl616/aidis-lab-homepage (main).
- 추가 자료 의존성은 자동 실행 TODO가 아니다. 새 요청 없이 기능 확대·커밋·push·배포하지 않는다.

## 다른 PC의 새 채팅에서 시작

1. 프로젝트 루트의 AGENTS.md와 이 문서를 읽고 현재 파일과 대조한다.
2. git status --short --branch, git log -3 --oneline, git diff --stat, git ls-files --others --exclude-standard로 기존 변경을 확인한다. git ls-remote origin refs/heads/main으로 실제 원격도 확인한다. 무조건 pull/reset하지 않는다.
3. 빌드·npm 설치는 필요 없다. Python 3가 있으면 루트에서 python -m http.server 4173 --bind 127.0.0.1로 미리보기를 실행한다. 다른 환경에서는 사용 가능한 정적 서버를 사용한다. 이번 PC에서는 python 명령이 실행되지 않아 임시 Node 서버를 사용했으며, 그 서버나 경로가 다른 PC에 유지된다고 가정하지 않는다.
4. http://127.0.0.1:4173/index.html에서 시작한다. 포트가 사용 중이면 서버 루트를 먼저 확인하고 기존 세션을 임의 종료하지 않는다. 브라우저·임시 서버·도구·절대 경로는 이식되지 않는다.
5. 작업 대상 코드와 README.md를 확인한다. 교수·연구 사실을 수정할 때는 CONTENT-AUDIT.md도 읽는다. 공통 요소 변경은 HTML 23개 전체를 대조한다.

시작 문구: “AGENTS.md와 CODEX_CONTEXT.md를 읽고 실제 프로젝트 상태와 비교한 뒤 이전 작업을 이어갈 준비를 해줘.”

## 구조와 파일

대전대학교 AIDIS Lab의 빌드 없는 HTML/CSS/순수 JavaScript 홈페이지다. 프레임워크·빌드 의존성은 없다. 새 활동은 Supabase Auth/Database를 사용하며 SDK 2.116.0을 로컬 vendor 자산으로 포함한다. 실행을 위한 별도 설치나 빌드는 필요 없으며 Python은 미리보기 용도다.

| 파일 | 역할 |
| --- | --- |
| `index.html` | 홈·페이지 안내·수상 소식·예정 일정 |
| `about.html` | 연구실 소개·활동 방향·위치/문의 |
| `professor.html` | 교수 학력·경력·연구·대표 논문 |
| `research.html` | 통합된 세 연구 분야 |
| `members.html` | 실제 재학생 7명·졸업생 5명, 검색·탭 |
| `member-ahn-chiwoo.html` | 랩장 상세, 경력/활동 9건 |
| `member-lee-taegyu.html` | 개인 상세, 경력/활동 4건 |
| `member-yu-nagyeong.html` | 개인 상세, 경력/활동 2건 |
| `member-jeong-seonju.html` | 개인 상세, 경력/활동 4건·자격증 |
| `member-yoon-jongmin.html`, `member-choi-seoyeong.html` | 추가 구성원 상세, 경력·활동 미제공 |
| `member-choi-suyeon.html` | 최수연 상세, 대학 연구실 활동 1건 |
| `activities.html`, `activity-kpaas.html` | K-PaaS 1건의 목록/필터, 기존 K-PaaS 상세 |
| `account.html`, `account.js`, `account.css` | 통합 로그인·프로필·활동·승인·콘텐츠·운영 설정 |
| `activity-admin.html`, `activity-admin.js`, `activity-editor.css` | 이전 관리 주소의 통합 화면 이동, 공통 편집 스타일 |
| `site-api.js`, `site-model.js`, `site-session.js`, `site-content.js`, `cms-catalog.json` | 권한 조회·안전한 CMS 필드·세션 메뉴·공개 콘텐츠 반영 |
| `activity.html`, `activity-detail.js` | 새 게시물 상세 |
| `activity-api.js`, `activity-model.js`, `activity-config.js` | Supabase 연결, 입력 검증·안전한 렌더링, 공개 연결 설정 |
| `SUPABASE_SETUP.md`, `supabase/migrations/` | 운영·권한·복구 절차와 적용한 DB 초기 SQL |
| `contact.html` | 연구실 위치·지도교수/랩장 공개 연락 경로 |
| `member-jeong-jaehyeong.html` | 졸업생 상세, KT TechUp 프로젝트 1건 |
| `members.js` | 구성원 자동 정렬·검색·탭·URL 상태 복원 |
| `activities.js`, `lab-updates.js`, `navigation.js` | 활동 필터, 공개 예정 일정, 기존 홈 해시 호환 |
| `styles.css` → `content.css` → `pages.css` | 공통/콘텐츠/페이지 스타일, 이 순서로 로드 |
| `assets/members/` | 실제 사진 6개와 `.gitkeep` |
| `CONTENT-AUDIT.md`, `README.md` | 출처 대조 기록, 실행/등록 안내 |
| `.github/workflows/pages.yml` | main push/수동 실행 시 루트 전체 Pages 업로드 |

- HTML은 현재 23개다. 헤더/푸터는 각 HTML에 복제되어 있다. 메뉴는 홈 → 소개 → 지도교수 → 연구 → 구성원 → 활동 → CONTACT와 로그인/마이페이지다.
- 모든 HTML의 CSS 버전은 `pages.css?v=20260918-8`. 구성원 스크립트는 `members.js?v=20260918-2`.
- `pages.css`에는 UI 반복 개선에 따른 후행 덮어쓰기 규칙이 있다. 앞쪽 규칙만 보고 판단하지 말고 끝부분·미디어 쿼리·브라우저 계산 스타일을 확인한다. CSS 구조 정리는 별도 요청 없이 수행할 필수 작업이 아니다.
- 로고/파비콘은 `assets/aidis-mark.svg`, 공유 이미지는 얼굴 없는 `assets/og-aidis-lab.png`. 교수 사진과 실제 학생 사진은 본문용이며 공유 이미지는 유지했다.


## 최종 UI 결정

### 공통·홈

- PC 최대 1200px 중앙 레이아웃, 헤더 88px, 하위 페이지 왼쪽 메뉴 200px·간격 32px. 1000–1199px는 메뉴 170px·간격 24px다.
- 하위 17페이지에 academic-layout·section-menu·academic-content를 사용한다. 본문 건너뛰기는 하위 #page-content, 홈 #main이다.
- 세부 메뉴는 실제 분류만 표시한다. 교수·연구는 왼쪽 제목만, 구성원은 재학생·졸업생, 활동은 전체·개발·공모전/수상·교육/운영·발표/공유다. 1000px 미만에서는 기존 본문 탭/필터를 쓴다.
- 홈 대표 카드 5개(연구·구성원·K-PaaS·활동·지도교수), Notice & News / Calendar 패널, 공통 중앙 정렬 푸터는 유지했다. 네트워크 장식 가장자리 잘림은 의도된 디자인이다.
- 공통 위치·연락처는 contact.html 및 각 HTML을 참조하며 이 문서에 복제하지 않는다.

### 구성원 목록

- 재학생 7명은 사용자 확인 컴퓨터공학과·1기이며 학년을 연도로 자동 계산하지 않는다. 카드 순서는 HTML 및 정렬 데이터가 기준이다.
- 랩장 우선 → 기수 → 입학 연도 오름차순. 0기도 유효하며 누락/빈/음수 값은 후순위, 동률은 원래 순서다.
- PC 사진 88×110px, 이름 22px. 1000–1199px에서는 80×100px·20px. 확인한 재학생 카드 높이는 각각 152px/138px로 동일하다.
- 인물 왼쪽, 기술 스택·관심 분야 오른쪽 두 행이다. **한 줄에 온전히 들어가는 태그만 표시**한다. 말줄임표·부분 잘림 없이, 넘는 태그는 통째로 숨긴다. 뒤의 짧은 태그가 남은 공간에 들어가면 표시될 수 있다.
- members.js의 fitMemberTags가 실제 너비를 측정한다. ResizeObserver·리사이즈·폰트 준비·탭/검색 변경 때 재계산하며 1000px 미만에서는 태그를 다시 모두 표시한다. 전체 텍스트는 HTML·검색·상세에 보존한다.
- 사진·이름·학적·배지 영역은 상세 링크다. 이름 옆 화살표는 없고 hover/키보드 포커스로 표시한다. 이름만 등록한 졸업생 4명은 상세 링크가 없다.
- 검색창 우측 연구실 문의는 PC에서 구성원 찾기 레이블과 상단 정렬한다. 검색 placeholder와 졸업생 제목 옆 설명 문구는 제거했다.
- 검색은 NFKC·대소문자 무시·공백 단어 AND 조건이다. q 파라미터, 결과 수·빈 상태·지우기·Escape·뒤로 가기 복원을 지원한다.
- 기본 재학생, 졸업생은 #alumni. 기존 #achievements도 졸업생 선택으로 호환한다. PC 사이드 메뉴와 본문 탭 선택을 동기화하며 본문 탭 방향키/Home/End를 지원한다. 인원수는 카드에서 계산한다.

### 개인 상세·교수

- 개인 상세 12개는 **학적·기수·소속·이메일·GitHub·기술 스택·관심 분야** 7개 공통 항목을 같은 순서로 표시한다.
- 사용자 지시로 미제공 소속은 AIDIS Lab, 나머지 미제공 공통 값은 —다. 졸업 예정은 학적 값에 포함한다. 상단 연구실 역할 및 랩장 표기는 제거했지만 목록 배지·경력의 역할은 유지한다.
- 기술 세부 분류는 기술 스택으로 합쳤고 모든 내용은 보존했다. 정선주 자격증은 경력·활동 아래 별도 섹션으로 이동했다.
- **최종 PC 배치:** 이름을 전체 너비 상단 왼쪽에 표시하고, 그 아래 사진 왼쪽·정보 오른쪽으로 배치한다. 오른쪽 열에 이름을 두거나 사진을 긴 본문의 세로 중앙에 맞추는 과거 방안은 폐기했다.
- 사진은 교수·개인 공통 220×275px, 1000–1199px에서 200×250px. 이름 30px, 개인 정보 14px·항목명 12px. 사진 원본은 수정하지 않고 object-fit: cover로 표시한다.
- 개인 상세에는 이름 아래 구분선을 유지한다. **교수만 이름 아래 구분선 없이 프로필 끝·학력 위에 구분선**을 둔다. 교수 소개 한 문장은 삭제했으며 학력·경력·연구 사실은 유지했다.
- 상세 내용의 더보기/접기는 구현하지 않았다. 긴 값은 상세에서 전체 줄바꿈 표시한다.

## 졸업생·사진 자료

- 졸업생 5명 중 정재형은 member-jeong-jaehyeong.html에 상세가 있다. 0기·2026 졸업. 2022 입학은 목록 정렬 데이터로만 보존한다. 소속은 공통 기본값 AIDIS Lab이다. 학과·취업처 등 미제공 사실은 추정하지 않는다.
- 나머지 4명은 이름과 기본 이미지만 표시한다. 이미지 성별 대응은 사용자 지정으로 members.html에 반영했다. 남녀 기본 SVG 2개를 추가했으며 가상 경력은 없다.
- 정재형의 KT TechUp 실무 프로젝트(구름공방, 2026.02–2026.04, 대상)를 개인 상세에 유지했다. 2026-09-21부터 활동 목록에는 개인 경력을 중복 표시하지 않는다. 기존 K-PaaS 성과와 참여 관계를 연결하지 않았다.
- 최수연 사진은 assets/members/choi-suyeon.png에 원본을 저장했고 목록·상세에 연결했다. 실제 학생 사진 6개이며 최서영·정재형은 사진 미제공으로 자리표시자를 사용한다.
- 홈페이지 등록용 사용자 제공 정보·사진은 항상 공개 동의 YES로 처리한다는 기존 지시를 유지한다. 재확인하지 않는다. 연락처·개인 정보는 해당 HTML에서 관리하고 이 문서에 복제하지 않는다.

### 기술·경력 편집 기준

- 기술 스택은 언어·프레임워크·플랫폼·AI 도구·버전 관리 기술. Visual Studio/VS Code는 목록·상세에서 제외했다. 기술 항목의 Git/GitHub는 Git으로 통일하되 실제 GitHub 링크와 활동 설명은 유지한다. 랩장 관심 분야의 클라우드 컴퓨팅은 제거했다.
- 개인 경력/활동은 진행 중 우선, 같은 상태에서 마지막 확인 연월 최신순이다. 날짜 미제공은 후순위, 같은 날짜는 기존 순서 유지. **현재 HTML에 정렬해 저장한 것이며 개인 활동 자동 정렬 스크립트는 없다.** 추가 시 직접 같은 규칙으로 배치한다.
- `activity-N` ID는 순번을 재부여하지 않는다. 기존 외부·공유 링크의 호환성을 위해 순서 변경 시에도 보존한다.
- 랩장 상세의 일영/GDGoC 참여는 `2025.09 - 현재`. 회장/Organizer 1년 임기는 `2025.09 - 2026.08`, 임기 종료 후 현재 운영진으로 참여한다. 전환 월은 별도 추정하지 않았다. 역할 이력·활동 목록 요약에 반영했다.
- `member-ahn-chiwoo.html#activity-5` 제목은 ‘AIDIS Lab 연구실’이다. `#activity-7` ZeroWaste는 사용자 확인으로 **2025.07**로 프로필에 유지한다. 다른 인물의 유사 프로젝트 날짜를 임의로 변경하거나 같은 팀이라고 연결하지 않았다.


## 활동·홈·기존 연구 내용

- 활동 목록은 K-PaaS 1건과 Supabase에서 관리자 승인된 새 활동을 표시한다. 개인 경력 21건은 목록에서 제거했고 개인 프로필에는 보존했다. 현재 `competition` 1건, 나머지 분류 0건. 필터·건수·빈 상태 지원.
- 새 활동은 등록 시각 최신순으로 분류별 20건씩 읽는다. K-PaaS는 정적 HTML로 남겨 API 장애 때도 접근 가능하다. 개인 프로필 경력을 활동 목록에 자동 취합하지 않는다.
- 관리자 글은 제목·분류·선택 활동일·요약·본문·선택 관련 링크로 구성한다. 본문은 일반 텍스트이며 HTML을 실행하지 않는다. 삭제는 deleted_at 설정으로 숨기며 복구 절차는 SUPABASE_SETUP.md에 있다.
- activity_admins의 관리자 UID를 DB RLS에서 검증한다. 일반 가입은 활성화했고 익명 로그인은 비활성화했다. 새 테이블은 명시적 GRANT와 RLS로만 공개한다. 비밀 키는 사용하지 않는다. 설정 값과 사용자 계정 정보는 이 문서에 복제하지 않는다.
- 초기 SQL은 Supabase SQL Editor로 적용했으며 원격 migration history는 아직 미등록이다. 같은 SQL을 재적용하지 않고 운영 문서를 따른다.
- K-PaaS 수상은 졸업한 학부연구생들의 재학 중 성과·상금 200만원. 새 인물과 참여 관계가 확정되지 않아 연결하지 않았다. 정확한 수상일·결과물 링크·개인 기여는 추정하지 않는다.
- 홈 Notice & News는 정적 HTML의 K-PaaS 소식 1건이다(뉴스는 최대 3건을 유지하는 편집 기준이며 자동 제한 기능은 없다). Calendar는 일정 목록으로 월간 달력 UI가 아니다. `labEvents = []`이며 실제 일정은 없다. 일정 입력 형식은 `{title, date, location, url}`, 날짜 `YYYY-MM-DD`. 한국 시간 오늘 포함 미래만 날짜순 최대 3건 표시한다. 일정 작성 화면이나 캘린더 연동은 없다.
- 소개는 본문 소제목·문단 중심의 한 열 구성이고 마지막 함께하기 영역은 PC 두 열이다.
- 연구 분야는 데이터 분석 및 인사이트 도출 / 인공지능 모델 연구 / AI 기반 소셜 플랫폼 개발. 교수 주요 연구 분야는 MANET·Routing Protocol / 소셜 빅데이터 분석·SNA / AI로 별도 유지한다.
- 교수는 학력 4건, 현재 경력 7건, 펼칠 수 있는 이전 경력 11건, 연구 7건(진행 2·완료 5), 대표 논문 6편. 목차형 바로가기는 제거했지만 기존 섹션 ID와 기간 표기는 유지한다.
- 국방 R&D 전체 과제 ‘인공지능 체계 신뢰성 시험 표준화 연구’와 대전대 담당 ‘인공지능 영상 신뢰성 평가 표준화 연구’를 중복 집계하지 않는다. 국방대 주관·고려대 자율주행·대전대 영상 AI 역할은 사용자 확인이다. `2025.09 - 현재`는 사용자 지정 표기이며 계약상 정확한 기간/기관은 확인 대상이다.
- 자료 근거는 `CONTENT-AUDIT.md`를 읽는다. 교수 경력/오픈랩의 현재 여부는 기존 자료 기준으로 실시간 검증된 사실이 아니다. 대표 6편과 과거 전체 41편 대조 기록을 구분한다.
- 기존 홈 해시 about/professor/research/members/contact는 개별 페이지로, projects는 `professor.html#projects`로 연결한다.


## 남은 자료 의존성

- 추가 인물·활동 및 사진은 새 자료 제공 후 반영한다. 현재 요청의 차단 요소나 자동 작업 목록은 아니다.
- 랩장 교직과정/연구실 활동 시작 시점, 구체적 연구실 성과, Cloud Study Jam 공식 과정별 수료·배지는 미확인이다. 개인 학습 완료를 공식 수료로 확대하지 않는다.
- 이태규 수상 2건의 연도·역할·연결 프로젝트는 미제공이다. 교수 연구 계약 기간·기관과 현재 경력/오픈랩 최신 상태는 필요 시 출처 대조한다.
- 기존 참고 PPT 상대 경로는 ../hompage-ref/2026_aidis_홍보데이(최종).pptx이며 자료일은 2026.03.19다. 이전에는 파일이 없었고 이번에는 존재를 재검증하지 않았다. 원본이 필요한 새 요청에서만 확인한다. Git에 포함하거나 비공개 원본을 임의 복사하지 않는다.

## 검증과 한계

- 2026-09-21 통합 계정: Node 테스트 7개, 전체 JS 구문, HTML 23개 내부 링크·앵커·중복 ID·CSS 순서·공통 로그인 메뉴, Git diff 공백 검사를 통과했다. `tests/site-permissions.sql`로 연결 전 제출 금지, 자기 승인 금지, 본인 프로필 허용/타인·사이트 수정 금지, 활동 자기 공개 금지, 비공개 글/신청 정보 격리, 관리자 승인·삭제, 운영 설정 변경 권한 및 꺼진 기능의 DB 차단을 검증하고 롤백했다. 관리자 화면에서 콘텐츠/프로필/설정 저장과 활동 복구·승인·삭제를 확인했다. 모바일 390px(내부375px)·좁은 PC 1024px(내부1009px) 프레임에서 가로 넘침 없음. 키보드 Tab으로 운영 설정 진입, 비로그인 화면, 관리자 콘솔 오류 없음을 확인했다. 임시 계정으로 실제 가입·로그인·로그아웃, 연결 승인 전 제출 금지, 연결 승인 후 본인 프로필 수정/타인 수정 차단, 활동 제출·자기 공개 차단·관리자 승인 전후 익명 공개 여부를 검증했다. 테스트 계정·요청·프로필·활동은 모두 정리했다. 사진 업로드·계정 복구의 전체 흐름은 아직 미검증이다. 아래 PC 검증 기록은 이전 구현 기준이다.

- 이번 작업에서 PC 목록 실제 CSS 너비 1025/1366/1920px의 카드 높이·태그 한 줄·부분 잘림 없음·이미지 로드·가로 넘침 없음을 확인했다. 화면 확대 비율로 요청 뷰포트와 innerWidth가 달라 실제 CSS 너비로 기록했다.
- 숨겨진 태그 검색, Escape 초기화, Enter 상세 이동, 졸업생 탭·0기, 활동 개발 6건과 새 상세 앵커를 확인했다.
- 최종 재배치는 교수·개인 상세 9개 × 약 1025/1366px 18조합에서 이미지·가로 넘침을 검사했다. 마지막 교수 구분선 이동은 교수 화면 및 계산 스타일로 확인했다.
- 공통 항목 7개 동일성, 자격증 하단 이동, 소개글 제거를 확인했다. 이전 단계에서 모바일 기본 탐색을 검사했지만 최종 PC 조정 전체의 모바일 검증을 대체하지 않는다.
- 마무리 시 내부 파일·앵커·중복 ID·CSS 로드 순서/버전·JS 구문·git diff --check 검사를 통과했다. 상설 테스트 스위트나 빌드 의존성은 없다.
- 실기기·다른 브라우저 엔진·스크린리더·메신저 공유 캐시·실제 일정이 등록된 화면은 미검증이다. 외부 링크 내용이나 교수의 현재 경력을 이번에 다시 검증한 것은 아니다.
- 다음 UI 변경 시 요청 화면·키보드·링크/앵커·고정 헤더 가림·가로 넘침을 확인한다. 활동 필터 해시는 DOM 앵커가 아닌 상태값임을 정적 검사에서 구분한다.

## 배포 상태와 문서 취급

- 구현 3a80956 main push 완료. [배포 실행 35302395328](https://github.com/ClCl616/aidis-lab-homepage/actions/runs/35302395328) 및 Pages 실행 35302394740 모두 success. 공개 HTML 16개·CSS 3개·JS 4개·신규 이미지 3개 총 26개가 로컬과 일치했다(텍스트 줄바꿈 정규화, PNG 바이트 동일).
- 후속 문서 커밋 6159479의 Actions 실행 35302485398 및 Pages 실행 35302484955 성공을 2026-09-21 확인했다. 현재 새 기능은 사이트에 미배포이며 Supabase 프로젝트와 DB 설정만 실제 원격에 적용했다.
- .github/workflows/pages.yml은 main push/수동 실행 때 루트 전체를 Pages에 업로드한다. 문서만 push해도 배포가 실행된다.
- 소스 커밋·원격 반영·Actions 성공·공개 파일 반영은 각각 구분한다. 문서도 공개되므로 인증정보·개인 연락처 모음·임시 검사 파일은 넣지 않는다.

## 주요 이력

- 2026-09-09~15: 다중 페이지, 교수 출처 대조, 실제 재학생 7명·활동·검색 구현. PC 홈·카테고리·푸터 개선 배포. 이전 기준 커밋 a7d35a6.
- 2026-09-18: 졸업생 5명·상세 1개·활동 1건·최수연 사진 추가. PC 목록 태그 요약, 개인 상세 공통 항목, 교수·구성원 배치 최종 확정. 마무리하며 과도한 중간 기록·폐기된 배치 설명을 통합했다.

- 2026-09-21 추가 검증: Security Advisor 오류 0건. 프로젝트 자동 생성 함수 rls_auto_enable의 불필요한 공개 실행 권한을 제거했다. 유출 비밀번호 검사 미사용 경고 1건은 남아 있다. 대시보드에서 Pro 이상 전용 기능임을 확인했고 요금제는 변경하지 않았다.

## 통합 관리의 유지보수 유의점

- `site_pages.values`와 정적 HTML 기본값을 구분한다. DB 수정은 일반 브라우저에 적용되지만 JavaScript 비활성화·네트워크 실패 시 정적 초기값을 표시한다. 메타데이터·페이지 구조·홈 일정 데이터는 소스 관리다. `cms-catalog.json`의 키와 HTML `data-cms`를 임의 재번호하지 않는다. `cms-text`는 기존 강조·링크 주위 텍스트를 스타일 간섭 없이 편집하기 위한 인라인 요소다.
- 이름만 있던 졸업생 4명의 상세 페이지를 추가했다. 기존 공개 이름·기본 이미지만 사용했으며 미제공 항목은 —다. 모든 프로필의 추가 소개는 입력 전 숨겨진다.
- 최초 활동 migration만 원격 history에 미등록이다. 후속 migration은 MCP 적용 후 원격 버전으로 로컬 파일명을 맞췄다. 운영 절차는 `SUPABASE_SETUP.md`를 참조한다.
- 세션은 localStorage에서 페이지·탭 간 공유한다. `activity-api.js` import 경로는 하나로 통일해 클라이언트 중복 생성을 피한다.
- 비밀번호 재설정 메일 버튼은 SMTP 미설정으로 숨겼다. 분실 계정은 프로젝트 운영자에게 문의한다. 실제 이메일 소유 확인 없이 가입하려는 요구이므로 관리자가 실구성원 확인 후 연결해야 한다.

- 2026-09-21 최종 가입 검증: 사용자가 이메일 확인을 직접 비활성화한 뒤 `tests/site-auth.integration.mjs --run`을 실행해 실제 Auth/REST 통합 검증을 통과했다. 임시 데이터 제거 후 CMS 페이지 20개·새 공개 활동 0건·테스트 계정 0개를 확인했다. 이후 사용자가 배포를 요청하여 배포 작업에 포함했다.

- 2026-09-21 배포: 사용자 요청으로 기능 커밋 d052111을 main에 push하고 Pages 배포 성공(실행 35530349259)을 확인했다. 실제 공개 로그인 화면 및 주요 자산 응답을 검증했다. 관리자·구성원·비로그인 역할별 수동 테스트 순서는 사용자에게 안내했다.
