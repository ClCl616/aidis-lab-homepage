# Project Context

최종 갱신: 2026-09-10 (한국 시간)
기준: 실제 프로젝트 파일과 Git 상태. 이 문서는 인수인계용이며 코드가 우선한다. Git/배포 상태는 아래 기록 시점의 스냅샷이다.

## 새 PC에서 바로 시작하기

1. 저장소를 clone하거나 기존 작업 폴더의 변경을 확인한 뒤 main을 pull한다. `git status --short --branch`와 `git log -3 --oneline`으로 현재 상태를 확인한다. 로컬 변경을 덮어쓰지 않는다.
2. `AGENTS.md` → 이 문서 → 필요한 `README.md`, `CONTENT-AUDIT.md`와 코드를 읽는다.
3. Python 3가 있다면 루트에서 `python -m http.server 4173 --bind 127.0.0.1` 실행. Windows는 `py -3`, 다른 환경은 `python3`도 사용 가능하다. 별도 설치/빌드는 없다.
4. `http://127.0.0.1:4173/`에서 확인한다. 기존 PC의 서버·브라우저·런타임 경로가 유지된다고 가정하지 않는다. 포트 사용 중이면 서버 루트부터 확인한다.
5. 다음 작업은 실제 구성원 자료와 공개 승인을 받아 샘플을 교체하는 것이다. 사용자는 현재 자료를 취합 중이다. 임의로 실제 학생 정보를 가져오지 않는다.

## 프로젝트와 구현 구조

대전대학교 AIDIS Lab 공개 홈페이지. 빌드 없는 HTML/CSS/순수 JavaScript이며 프레임워크·npm 의존성·백엔드·DB·관리자 편집 화면은 없다. 파일 편집 → 로컬 검토 → 사용자 업로드 요청 후 커밋/push하는 방식이다.

| 파일 | 역할 |
| --- | --- |
| `index.html` | 대표 화면·페이지 안내, 소식과 활동, 다가오는 일정 |
| `about.html` | AIDIS 의미, 연구·개발 방향, 함께하는 활동, 위치·문의 |
| `professor.html` | 교수 소개·학력·경력·주요 연구 분야·진행/완료 연구·대표 논문 |
| `research.html` | 통합된 세 가지 연구 분야 |
| `members.html` | 재학생/졸업생 탭과 예시 구성원 목록 |
| `member-kim-haneul.html`, `member-yun-seojin.html` | 재학생 개인 상세 샘플 |
| `member-park-doyun.html`, `member-lee-jian.html` | 졸업생 개인 상세 샘플 |
| `activities.html` | 개발 / 공모전·수상 / 발표·공유 유형 필터와 활동 목록 |
| `activity-kpaas.html` | 확인된 K-PaaS 공모전 수상 상세 기록 |
| `contact.html` | 연구실 위치·공개 연락 경로 |
| `navigation.js` | 기존 홈 해시 주소 호환 |
| `members.js` | 구성원 탭, 키보드, 해시·뒤로 가기 처리 |
| `activities.js` | 활동 유형 필터·건수·빈 상태 처리 |
| `lab-updates.js` | 공개 예정 일정 데이터와 표시 로직 |
| `styles.css` → `content.css` → `pages.css` | 순서대로 로드하는 공통/콘텐츠/최신 페이지 스타일 |
| `assets/aidis-mark.svg` | 전용 로고·파비콘 |
| `assets/og-aidis-lab.png` | 얼굴 없는 공유 이미지 |
| `assets/professor-park-gunwoo.png` | 교수 본문 사진 |
| `assets/member-placeholder.svg` | 예시 구성원 사진 자리 |
| `assets/members/` | 승인된 실제 사진을 넣을 디렉터리(현재 .gitkeep만 있음) |
| `CONTENT-AUDIT.md` | 교수·연구·학생 성과의 출처와 확인 범위 |
| `README.md` | 실행 및 콘텐츠 등록/교체 안내 |
| `.github/workflows/pages.yml` | main push 시 루트 전체를 GitHub Pages로 배포 |

현재 HTML은 12개다. 헤더·푸터는 각 파일에 복제되어 있어 공통 변경 시 모두 확인한다. 메뉴는 **홈 → 소개 → 지도교수 → 연구 → 구성원 → 활동 → CONTACT**. 홈 메뉴와 AIDIS LAB 로고 모두 index.html로 연결한다.

## 현재 화면과 합의된 결정

- 홈은 첫인상과 탐색, 소개는 연구실 이야기 역할이다. 소개의 대형 로고 배너·세 연구 카드가 홈과 겹친다는 피드백을 받아 제거했다. 소개는 간결한 두 열 편집, 모바일 한 열 구성이다.
- 연구 분야: 데이터 분석 및 인사이트 도출 / 인공지능 모델 연구 / AI 기반 소셜 플랫폼 개발. 명칭·제안 설명을 관련 페이지에 통일했고 영상 AI·신뢰성 상세 설명은 유지했다. 연구 페이지의 연구과제 이동 버튼은 삭제했다.
- 교수: 학력 4건, 현재 경력 7건, 접을 수 있는 이전 경력 11건, 연구 7건(진행 2·완료 5), 대표 논문 6편. 주요 연구 분야는 기존 공식 정보인 MANET·Routing Protocol / 소셜 빅데이터 분석·SNA / AI를 독립 섹션으로 표시한다.
- 교수 페이지 바로가기는 학력·경력·주요 연구 분야·진행 중인 연구·완료된 연구·대표 논문. 연구 분야 연결 문구는 ‘연구 분야 상세보기’다.
- 연구과제·경력 기간은 `YYYY.MM - YYYY.MM` 또는 `YYYY.MM - 현재`로 통일했다. 학력의 연도/여름 표기는 유지한다.
- 구성원은 재학생 2명·졸업생 2명 모두 가상 예시. 기본 재학생 탭, 선택한 그룹만 표시. 방향키·Home/End로 탭 전환, 기존 해시와 뒤로 가기 지원. 교수 소개 띠와 졸업생 활동 영역은 구성원 페이지에서 제거했다. 문의 버튼은 ‘문의하기’다.
- 목록 카드: 사진 자리·이름·입학 연도·한 줄 소개·기술 스택·관심 분야·‘상세보기’. 화면 링크는 이름을 생략하지만 aria-label에는 이름을 유지한다.
- 개인 상세: 사진 자리·입학 연도·예시 이메일·기술 스택·관심 분야·가상 경력/역할/기여·외부 링크 자리·연구실 활동 연결 영역. 가상 안내와 noindex 메타를 적용했다. GitHub/포트폴리오는 실제 주소가 없어 비활성 텍스트다. 실제 성과와 연결하지 않았다.
- ‘프로그래밍 언어’ 항목명은 사용자 선택으로 **기술 스택**으로 바뀌었다. 언어·프레임워크·도구를 수집할 수 있으나 샘플 기술 값은 그대로다.
- 활동은 확인된 K-PaaS 수상 1건만 등록. 개인·팀 개발/발표는 구조와 필터만 있으며 실제 자료 대기. 이름·기여·수상일·결과물 링크는 추정하지 않는다.
- 메인은 수상 소식 1건과 예정 일정 영역을 표시한다. labEvents는 빈 배열이라 예정 일정 없음 안내가 나온다. 내부 이슈 관리나 실제 일정이 구현/등록됐다고 설명하면 안 된다.
- 각 페이지는 title/canonical/OG/Twitter/favicon을 갖추고 공유 이미지는 동일하다. 샘플 프로필의 제목·URL은 개별 설정, noindex는 실제 자료 공개 승인 후 제거한다.

## 콘텐츠 근거와 확인 필요 사항

- 상세 근거는 `CONTENT-AUDIT.md`가 기준이다. 공식 교수 소개와 연구실 제공 PPT, 사용자 확인을 구분한다.
- 국방 R&D 전체 과제는 ‘인공지능 체계 신뢰성 시험 표준화 연구’. 사용자 확인으로 국방대 주관, 고려대 자율주행, 대전대 영상 AI 역할을 확정했다. 대전대 담당 연구가 ‘인공지능 영상 신뢰성 평가 표준화 연구’이며 전체 과제와 별도로 중복 집계하지 않는다.
- 영상 연구 기간 `2025.09 - 현재`는 자료에서 확정한 날짜가 아닌 사용자 지정 표기다. 정확한 계약 기간·기관 정보는 확인 필요. PPT의 ‘향후 3년 지속’을 전체 기간으로 해석한 기존 문구는 삭제했다.
- 교수 경력과 오픈랩의 ‘현재’는 기존 자료 표기다. 실시간 진행 여부는 미확인. 대표 논문 6편만 수록하며 전체 41편이라는 과거 대조 기록과 구분한다.
- K-PaaS 수상은 졸업한 학부연구생들의 재학 중 성과이며 상금 200만원. 교수 과제나 가상 인물의 실적으로 연결하지 않는다.
- 실제 구성원 정보·사진·공개 승인·랩장 공개 연락 수단은 미제공. 참고 PPT의 실제 학생 이름/사진은 사용하지 않았다.
- 교수 연구실과 랩 공간은 다르다. 위치·연락처는 공개 HTML을 참조하고 이 문서에 복제하지 않는다.
- 참고 자료는 프로젝트 밖 `../hompage-ref/2026_aidis_홍보데이(최종).pptx`(자료일 2026.03.19). Git clone으로 따라오지 않는다. PPT 2쪽 연구경력은 이미지, 9쪽 랩 소개, 11쪽 학생 수상, 12쪽 영상 연구다. 원본 없이도 코드·자료 대조 기록으로 대부분 작업 가능하다.

## 다음 작업: 실제 구성원 등록

사용자에게 받을 항목:
- 이름, 재학생/졸업생, 입학 연도, 사진, 공개용 이메일, 기술 스택, 관심 분야.
- 경력·활동별 기간, 제목, 역할, 주요 기여·성과. GitHub와 포트폴리오 실제 링크.
- 선택: 한 줄 소개, 연구실 기수/합류 연도, 졸업 연도, 현재 소속, 랩장 역할·공개 연락 수단.
- 정보와 사진의 홈페이지 공개 동의.

등록할 때 목록과 상세를 함께 교체한다. 미제공 선택 항목은 생략한다. 파일명·title·canonical·OG URL·사진 alt를 실제 정보와 맞추고, 승인 후 예시 표기·가상 경력·예시 이메일·noindex를 제거한다. 공개 확인된 개인 활동만 상세에 연결한다. 자료가 없는 상태에서 기존 가상 이름을 실제 사람으로 해석하지 않는다.

활동/소식/일정은 추가 자료를 받으면 README 절차에 따라 등록한다. 공개 일정은 lab-updates.js의 labEvents 배열에 `{title, date, location, url}` 형식으로 추가한다. date는 YYYY-MM-DD, 오늘(한국 시간) 포함 미래 일정만 날짜순 최대 3건 표시. 지난 일정은 배열에 남지만 메인에서 제외된다. 자동 관리 UI·캘린더 연동·학생 직접 작성 기능은 없다.

## 실행과 검증

- 로컬 서버: `python -m http.server 4173 --bind 127.0.0.1`(또는 py -3 / python3). 프로젝트 실행 의존성은 없으며 Python은 미리보기 용도다.
- CSS 버전은 현재 모든 HTML에서 `pages.css?v=20260910-6`. HTML 변경 확인 시 고유 review 쿼리를 사용하고 HTTP 응답과 파일을 대조한다.
- 메뉴/로고/문의 링크, 교수 앵커, 키보드 details, 구성원 탭·상세 왕복·뒤로 가기, 활동 필터 빈 상태·상세 이동을 확인한다.
- 기존 홈 해시 about/professor/research/members/contact는 새 페이지로, projects는 professor.html#projects로 이동한다. 구성원 #alumni는 졸업생 탭, 제거된 #achievements는 졸업생 탭 선택만 유지한다.
- 데스크톱 1440px, 모바일 390·320px에서 줄바꿈·고정 메뉴 가림·가로 넘침·키보드 포커스를 확인한다. 내부 href/src는 쿼리와 해시를 분리하여 파일과 대상 ID 존재를 검사한다.
- 이전 단계에서 12페이지 화면/링크와 상세 4명 왕복 검증, 활동 필터, 일정의 지난 날짜 제외·정렬·3건 제한을 확인했다. 브라우저 임시 일정만 사용했고 실제 일정 배열은 비어 있다.
- 2026-09-10 최종 업로드 전 검증 결과는 최근 기록에 남긴다. 검증은 Playwright와 설치된 Edge로 수행했으며 프로젝트에 테스트 의존성을 추가하지 않았다. 임시 검증 스크립트/스크린샷은 Git 밖에 있어 다른 PC로 전달되지 않는다. 자동 테스트 스위트는 없고 다른 브라우저 엔진·실기기·스크린리더·메신저 캐시는 미검증이다.
- `git diff --check`, 스테이징 후 `git diff --cached --check`를 확인한다.

## Git·배포·이식

- 브랜치 main, 원격은 `git remote -v`로 확인. 공개 주소는 HTML의 canonical과 저장소 Pages 설정을 기준으로 확인한다.
- 구현 커밋 `0aaac2f`를 main과 origin/main에 반영했다. 구성원 탭/샘플·활동·소식·일정·홈 메뉴와 인수인계 문서를 포함한다. 해당 커밋의 GitHub Actions 배포 2건 모두 성공했고 공개 HTML 12개가 로컬과 일치함을 확인했다. 이 배포 결과를 기록한 후속 문서 커밋이 있으므로 최신 커밋은 git log와 Actions로 확인한다.
- `.github/workflows/pages.yml`: main push / 수동 실행 → checkout → Pages 설정 → 루트 전체 업로드 → deploy. **main push는 공개 배포를 유발한다.** 소스 push와 배포 성공은 별도 상태다.
- AGENTS.md와 이 문서는 Git 추적 대상이며 clone/pull로 함께 전달된다. 루트 전체 배포이므로 Markdown도 공개될 수 있다. 비공개 원본·개인정보·인증정보·검증 임시 파일을 저장소에 넣지 않는다.
- 이번 업로드 승인은 이번까지의 변경에 적용한다. 이후 수정의 커밋/push/배포는 다시 사용자 요청이 필요하다.
- 새 PC에서 기존 작업이 있으면 상태 확인 후 pull, 새 작업이면 clone한다. USB 이동은 숨김 .git과 미추적 파일 포함 여부를 별도로 확인한다. Codex 대화나 로컬 서버가 자동 복제되는 방식은 아니다.

## 최근 기록

- 2026-09-09: 다중 페이지·전용 로고·교수 자료 대조와 연구 통합. 인수인계 규칙 도입.
- 2026-09-10: 소개와 홈 역할 분리, 교수 주요 연구 분야·국방 R&D 역할 구분·기간 형식 개선. 이전 업로드 `8b74b11`.
- 2026-09-10: 구성원 탭과 가상 개인 상세 4개, 기술 스택 항목, 활동 목록/상세와 메인 소식·일정, 공통 홈 메뉴 구현. 실제 구성원 취합 대기.
- 2026-09-10 업로드 전 최종 검증: 12페이지 × 1440/390/320px, 내부 참조 238개, 메뉴·CSS 순서·중복 ID·응답/파일 일치·가로 넘침·교수 앵커·구성원 키보드 탭/상세 왕복·활동 필터/상세·이전 홈 해시 이동 통과. 브라우저 JavaScript 오류 없음. git diff --check 통과.

- 2026-09-10 업로드 완료: 구현 0aaac2f push 및 GitHub Pages 배포 성공, 공개 HTML 12개와 로컬 파일 대조 통과. 이 기록은 배포 결과를 남기는 후속 문서 변경이다.
