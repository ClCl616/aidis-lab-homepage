# 계정·콘텐츠 운영

## 구조와 권한

정적 HTML/CSS/JavaScript와 Supabase Auth/Database/Storage를 사용한다. 빌드나 서버 프레임워크는 필요 없다. 공개 연결 정보는 `activity-config.js`를 참조하며, secret/service_role 키는 사이트에 넣지 않는다. SDK 2.116.0은 `assets/vendor/`에 고정되어 있다.

- 비로그인: 공개 페이지와 승인된 활동을 읽는다.
- 가입 계정: 구성원 프로필 연결을 신청한다. 신청만으로 수정 권한을 얻지 않는다.
- 승인된 구성원: 본인 프로필의 문구·사진·추가 소개를 수정하고 활동을 제출한다. 제출한 활동은 관리자 승인 전까지 작성자와 관리자만 읽는다.
- 관리자: 구성원 연결 승인·해제, 모든 프로필과 사이트 본문의 문구·링크·사진 수정, 활동 승인·반려·수정·삭제·복구, 운영 설정 변경.
- 관리자 권한은 프로젝트 소유자가 `activity_admins`에 직접 부여한다. 사이트에서 자기 승격하거나 다른 계정에 관리자 권한을 부여할 수 없다.

`account.html`에서 통합 관리한다. 이전 `activity-admin.html` 링크는 통합 화면으로 이동한다. 로그인은 localStorage에 저장되어 같은 사이트의 페이지·탭에서 공유되며 로그아웃할 수 있다. 기존 탭 세션은 최초 접근 때 이전된다.

## 가입과 계정 연결

요구사항은 이메일 인증 없이 회원가입 후 관리자가 실제 구성원을 확인하여 연결하는 방식이다. 이메일 소유가 인증되었다고 판단하지 말고 별도 확인 후 연결한다. 익명 로그인은 허용하지 않는다. 이메일 발송 서비스는 연결하지 않았으므로 비밀번호 분실 시 프로젝트 운영자가 계정을 복구한다. Confirm email은 사용자가 비활성화했고, 인증 메일 없이 신규 가입과 로그인을 실제 API로 검증했다.

관리자의 구성원 승인 탭은 신청 계정과 원하는 프로필을 표시한다. 한 계정과 한 프로필은 각각 한 번만 연결할 수 있다. 잘못 승인했다면 연결 해제 후 다시 연결한다. 반려 요청도 관리자가 다시 승인할 수 있다.

## 운영 설정

관리자 전용 운영 설정의 네 항목은 `site_settings`에 저장되며 RLS에서도 적용된다.

1. 프로필 연결 신청 접수
2. 구성원 본인 프로필 수정
3. 구성원 활동 제출
4. 승인된 새 활동 공개 표시 (기존 K-PaaS 정적 기록 제외)

관리자는 본인 프로필 수정·제출 제한과 관계없이 콘텐츠를 관리한다. 설정은 회원가입 자체나 Supabase 인증 정책을 바꾸는 기능이 아니다. 활동은 항상 관리자 승인 후 공개된다.

## 콘텐츠 편집과 공개

`cms-catalog.json`은 기존 공개 HTML의 편집 가능한 필드와 초기값을 정의한다. HTML의 `data-cms` 키를 임의로 재번호하지 않는다. `site_pages.values`에는 수정 값을 저장하고 공개 페이지가 적용한다. 기본 HTML을 보존해 통신 장애나 JavaScript 미사용 시 초기 콘텐츠를 읽을 수 있다. 따라서 DB 수정값이 검색엔진·공유 카드·스크립트 없는 화면까지 보장되지는 않는다. 메타데이터·레이아웃·새 페이지·일정 구조 변경은 소스 작업이다.

기존 개인 경력 앵커는 유지한다. 추가 소개에서 새로운 개인 이력을 작성할 수 있다. 활동 목록은 K-PaaS와 승인된 새 활동만 표시한다. 구성원 프로필 저장값은 목록의 이름·사진·학적·기수·기술·관심 분야에도 반영한다.

사진은 `profile-photos` 공개 버킷의 페이지별 경로에 새 이름으로 업로드한다. JPG/PNG/WebP, 최대 5MB이며 승인된 본인이나 관리자만 업로드할 수 있다. 업로드 후 저장해야 적용된다. 이전 이미지 파일은 자동 영구 삭제하지 않는다.

본문은 textContent로 출력하고 URL 프로토콜을 검증한다. 저장은 `updated_at`을 비교해 다른 사람의 변경을 덮어쓰지 않는다. 실패하면 입력을 유지한다.

## 활동 삭제·복구

삭제는 `deleted_at`을 설정하는 보관 방식이다. 관리자 화면에서 복구하면 승인 대기로 돌아가며 다시 승인해야 공개된다. 브라우저에는 영구 삭제 권한을 부여하지 않는다.

## DB 변경 이력

- `20260920152033_activity_publishing.sql`: 최초 대시보드 SQL 적용. 원격 migration history 미등록이므로 기존 프로젝트에서 재실행하지 않는다.
- 이후 세 migration은 Supabase MCP로 적용하고 로컬 파일명을 원격 history 버전과 맞췄다. 사이트 페이지 초기 행도 포함한다.
- 새 프로젝트에서는 migration을 순서대로 실행한다. 기존 프로젝트에서 CLI push 전에는 최초 migration의 이력을 실제 스키마와 대조해 정리한다.

## 검증

- `node --test tests/*.test.mjs`: 입력·URL·안전한 텍스트 렌더링 검증.
- `tests/site-permissions.sql`: 임시 사용자와 데이터를 만들고 연결 승인·본인 소유·자기 승격 차단·활동 승인·삭제·기능 설정의 DB 권한을 검증한 뒤 전체 롤백한다.
- Security Advisor 오류 없음. 유출 비밀번호 보호 미사용 경고는 Free 플랜에서 남아 있으며 요금제는 변경하지 않았다.
- `tests/site-auth.integration.mjs --run`: 실제 신규 가입·로그인·소유권·활동 승인 공개를 확인하는 수동 통합 검사. 임시 페이지 생성, 단계별 승인 및 종료 후 테스트 데이터 정리가 필요하며 일반 단위 테스트에 포함하지 않는다.
- 실제 화면과 API의 최신 검증 범위는 `CODEX_CONTEXT.md`를 참조한다.

참고: [RLS](https://supabase.com/docs/guides/database/postgres/row-level-security), [SMTP 제한](https://supabase.com/docs/guides/auth/auth-smtp), [비밀번호 보안](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection).
